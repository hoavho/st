# Required Fonts to run st
- NotoColorEmoji (`paru -S noto-fonts-emoji`)

# Dependencies needed to build st
```
sudo pacman -S pkg-config
```

# Build + Install st
```
cd st
sudo make clean install
```
